Drop real audio files here — filenames already match what index.html expects:

- track-1.mp3 (midnight drive)
- track-2.mp3 (slow static)
- track-3.mp3 (glass hallway)
- track-4.mp3 (no signal)

Also replace each card's placeholder gradient artwork in index.html (the `background:linear-gradient(...)` inline style on `.gate-art`) with a real `<img>` once you have artwork — same pattern as the other placeholder galleries on the site.

No JS changes needed either way.
